<view class="page-container">
  <!-- Header -->
  <header 
    brandName="{{brandName}}"
    showToggle="{{false}}"
    showBackArrow="{{true}}"
  />

  <!-- Address Section -->
  <view class="address-section">
    <view class="address-row">
      <text class="address-text">{{address}}</text>
      <text class="change-address" onTap="goToAddress">Cambiar dirección</text>
    </view>
  </view>

  <!-- Order Summary -->
  <view class="order-section">
    <text class="section-title">Resumen de tu pedido</text>
    
    <!-- Empty Cart Message -->
    <view class="empty-cart" a:if="{{cartItems.length < 1}}">
      <view class="empty-cart-icon">
        <view class="empty-icon-body"></view>
        <view class="empty-icon-wheel left"></view>
        <view class="empty-icon-wheel right"></view>
      </view>
      <text class="empty-cart-text">No tienes productos en el carrito</text>
      <view class="empty-cart-button" onTap="goToProducts">
        <text class="empty-cart-button-text">Ver productos</text>
      </view>
    </view>
    
    <!-- Cart Items -->
    <view class="cart-items" a:if="{{cartItems.length >= 1}}">
      <view 
        class="cart-item" 
        a:for="{{cartItems}}" 
        a:key="id"
      >
        <view class="item-image-container">
          <image class="item-image" src="{{item.image}}" mode="aspectFit" />
        </view>
        <view class="item-details">
          <text class="item-name">{{item.name}}</text>
          <text class="item-price">$ {{item.price}}</text>
        </view>
        <text class="item-remove" onTap="removeItem" data-id="{{item.id}}">Eliminar</text>
      </view>
    </view>
  </view>

  <!-- Empty Space -->
  <view class="spacer"></view>

  <!-- Invoice Question -->
  <view class="invoice-section">
    <text class="invoice-question">¿Desea factura electrónica?</text>
    <view class="invoice-buttons">
      <view class="invoice-btn {{wantsInvoice === true ? 'active' : ''}}" onTap="setInvoice" data-value="{{true}}">
        <text class="invoice-btn-text">Si</text>
      </view>
      <view class="invoice-btn {{wantsInvoice === false ? 'active' : ''}}" onTap="setInvoice" data-value="{{false}}">
        <text class="invoice-btn-text">No</text>
      </view>
    </view>
    
    <!-- NIT Input - Solo visible si wantsInvoice es true -->
    <view class="nit-container {{wantsInvoice === true ? 'show' : 'hide'}}">
      <text class="nit-label">NIT o Documento de identidad</text>
      <input 
        class="nit-input" 
        type="text" 
        placeholder="Ingrese NIT o documento"
        value="{{nitValue}}"
        onInput="onNitInput"
      />
    </view>
  </view>

  <!-- Bottom Payment Bar -->
  <view class="payment-bar">
    <text class="total-price">$ {{totalPrice}}</text>
    <view class="pay-button" onTap="handlePayment">
      <text class="pay-button-text">Pagar</text>
    </view>
  </view>
</view>
